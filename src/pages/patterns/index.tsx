import { Patterns } from 'wjhm';

export async function getStaticProps() {
  const props = {};

  return {
    props, // will be passed to the page component as props
  };
}

export default Patterns;
